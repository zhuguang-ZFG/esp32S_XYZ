---
description: Superpowers + 计划纪律 + 上下文过半新开窗口 + 收尾同步文档与 GitHub（本仓库全局）
alwaysApply: true
---

# esp32S_XYZ 全局代理规则

## Superpowers 原则（编码前必读）

1. **先技能、后动手**：只要任务可能与某 skill / 流程规范相关（哪怕约 1%），先加载并对照执行；流程类（规划、调试、TDD）优先于纯实现类 skill。
2. **先验证后实现**：无主板阶段以 M0（fake U1 / fake DeviceServer / schema / CI）收敛不确定性；能自动化验证的不要只靠手工试。
3. **严格按计划里程碑**：以 `docs/实施计划-v2.md` 顺序为准，语义以 `docs/架构定稿-v2.md` 为准；单任务粒度参考 `docs/编码任务索引-v2.md`；完成一步验收再进下一步。
4. **文档优先于代码**：文档与实现不一致时，**先修订 v2 与实施计划并写修订记录**，再改代码；禁止「先改代码、文档后补」。
5. **禁止盲目改代码**：只改当前子任务明确列出的文件/契约；禁止顺手重构、禁止扩大范围；无关文件不动。
6. **契约与字段演进**：涉及协议/字段时遵循「先 `docs/schemas/` 与 README，再改实现」（与实施计划 M0a / §19.5 一致）。

## 上下文长度 ≥ 50% 时（强制）

当本会话的上下文占用 **达到或超过约 50%** 时：

- **必须**提示用户 **新开 Chat / Composer 窗口** 继续，避免在长上下文中继续大改。
- 在新窗口首条消息中让用户或代理携带：**当前里程碑（如 M2.x）**、**未完成验收项**、**已修改文件列表**、以及 `docs/编码任务索引-v2.md` 对应小节链接或标题。

## 禁止事项（红线）

- 未读相关计划小节即跨多目录「一次性大改」。
- 跳过验收命令或 CI 即宣称完成。
- 为「省事」删除或弱化安全/鉴权相关已有约束（见架构 v2 红线章节）。

## 代码收尾：计划文档 + GitHub（强制）

每次**完成**与本仓库里程碑相关的编码后，在结束会话或宣称「做完」之前**必须**执行：

1. **更新计划文档**（与本次改动对应的子步一致）  
   - `docs/实施计划-v2.md`：对应 `M*.x` 的**状态**、**完成判定**、**阻塞/待验收**（若有）。  
   - `docs/编码任务索引-v2.md`：同一子步的**状态**与**验收命令**说明。  
   - 若语义与 `docs/架构定稿-v2.md` 冲突：先按仓库「文档同步规则」修订架构/计划，再保留代码结论。  
2. **同步远程**  
   - `git add` 本次代码与文档变更 → **一条清晰的中文或 conventional commit** → `git push` 到当前分支对应远程（用户未禁止推送时不得只改本地不推送）。

未完成 1+2 不得宣称本里程碑已交付。用户明确只要本地草稿、不要 push 时除外。
