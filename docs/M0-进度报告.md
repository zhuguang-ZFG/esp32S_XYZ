# M0 设计期验证 - 进度报告

**更新日期**：2026-05-14  
**状态**：进行中

## 总览

M0 是设计期验证阶段，目标是在没有主板的情况下，完成所有可以前移的验证工作。

```
里程碑 M0  设计期验证（无主板可做）
   ✅ M0a 契约固化（JSON Schema）
   ✅ M0b 静态检查（GPIO 冲突、strapping pin）
   ⏳ M0c 仿真器（fake U1、fake DeviceServer、fake provider）
   ⏳ M0d CI 与单元测试骨架（本地等价验证已完成，等待 GitHub required checks）
   ✅ M0e 基准修复（已完成）
   ⏸️ M0f 实物上电抽测（等实物，不阻塞 M1~M3）
```

## M0a 契约固化 ✅

**状态**：已完成

**产物**：
- `docs/schemas/README.md` - Schema 总览文档
- `docs/schemas/edge_a/` - Client capability 入参、事件出参、错误响应
- `docs/schemas/edge_b/` - motion_task / motion_event / intent_submit
- `docs/schemas/edge_c/` - DeviceServer 与 U8 之间的扩展 WSS 帧
- `docs/schemas/edge_d/` - U8↔U1 私有协议命令与响应

**完成判定**：
- ✅ 每条 capability、event、命令、响应都有 schema
- ✅ 有 README.md 说明命名和用途
- ✅ 后续字段变更遵循"先 schema 后实现"原则

## M0b 静态检查 ✅

**状态**：已完成并修复所有问题

**产物**：
- `tools/check_gpio.py` - GPIO 静态检查工具（Python）
- `tools/test_check_gpio.py` - 单元测试
- `tools/README.md` - 使用文档

**检查项**：
1. ✅ GPIO 冲突检测
2. ✅ Strapping pin 守卫
3. ✅ UART TX/RX 交叉连接验证
4. ✅ 模组未引出引脚检测
5. ✅ PADS 源文件证据提示（当前无弱证引脚）

**测试结果**：
- 8/8 单元测试通过
- ✅ 真实配置检查通过（0 错误，0 警告，0 信息）

**问题修复记录**：

初次检查发现：
- ❌ 3 个错误：U8 GPIO38/39/40 被误判为"模组未引出"
- ⚠️ 2 个警告：U1 GPIO46/45 strapping pin 未识别到"已知风险"标注

修复措施：
1. ✅ 更新 `UNAVAILABLE_PINS`：仅包含确认不可用的 IO35/36/37/41/42
2. ✅ 新增后续修订：`DLC_Motor_Control_P1_V1.0_260513.txt` 已证明 IO38/39/40 连接到 U8.31/32/33，`WEAK_EVIDENCE_PINS` 当前为空
3. ✅ 改进 strapping pin 检查：支持检查下一行注释中的"已知风险"标注
4. ✅ 新增 `_check_weak_evidence_pins()` 方法：对弱证引脚发出信息提示

**当前状态**：
- ✅ 0 错误
- ✅ 0 警告
- ✅ 0 信息提示（PADS 源文件已覆盖当前固件使用的 GPIO）

**完成判定**：
- ✅ 当前仓库可运行检查
- ✅ 故意加冲突能定位到行号
- ✅ 所有单元测试通过
- ✅ 真实配置无错误和警告

## M0c 仿真器 ⏳

**状态**：部分完成

### M0c.1 fake U1 ✅

**状态**：已完成

**产物**：
- `tools/fake_u1/fake_u1.py` - fake U1 仿真器（Python）
- `tools/fake_u1/test_fake_u1.py` - 单元测试
- `tools/fake_u1/README.md` - 使用文档

**功能特性**：
1. ✅ 完整状态机（IDLE/HOMING/RUNNING/PAUSED/ALARM/ERROR/ESTOP）
2. ✅ 支持所有命令（GET_STATUS/HOME/MOVE/PATH_*/PAUSE/RESUME/STOP/ESTOP）
3. ✅ 错误注入（E001/E005/E006/E008）
4. ✅ 响应延迟模拟
5. ✅ 工作区边界检查
6. ✅ 归零状态与位置跟踪

**测试结果**：
- ✅ 13/13 单元测试通过
- ✅ 覆盖正常流程与错误场景
- ✅ 协议格式符合 v2 §15 规范

**完成判定**：
- ✅ TCP 服务器可监听端口
- ✅ 接收 `@{json}\n` 格式命令
- ✅ 返回 ack/status/result/error 响应
- ✅ 状态机行为正确
- ✅ 错误注入可用

### M0c.2 fake DeviceServer ⏳

**状态**：待实现

**计划产物**：
- `tools/fake_device_server/` - fake DeviceServer（Python）

**优先级**：P2（BusinessServer 端开发依赖）

### M0c.3 fake AI provider ⏳

**状态**：待实现

**计划产物**：
- `tools/fake_ai/` - fake AI provider（Python）

**优先级**：P2（M3/M4 开发依赖）

## M0d CI 与单元测试骨架 ⏳

**状态**：本地等价验证已完成，等待 GitHub required checks

**已完成**：
- ✅ `.github/workflows/ci.yml` - CI 配置骨架
- ✅ `.github/markdown-link-check-config.json` - Markdown 链接检查配置
- ✅ `gpio-check` job - GPIO 静态检查
- ✅ `python-unit` job - Python 单元测试
- ✅ `schema-validate` job - JSON Schema 校验
- ✅ `fake-integration` job - Fake 环境集成测试
- ✅ `markdown-link-check` job - Markdown 链接检查
- ✅ `tools/validate_schemas.py` - schema 与样例一致性校验
- ✅ `tests/ci/` - CI 工具单元测试与 fixture

**待完成**：
- ⏳ GitHub 仓库 required checks 配置

**优先级**：P1（保证代码质量）

## M0e 基准修复 ✅

**状态**：已完成（2026-05）

**已完成项**：
- ✅ M0e.1: U8 `config.h` TX/RX 修复
- ✅ M0e.2: 硬件文档证据强度标注
- ✅ M0e.3: v1 文档失效声明

## M0f 实物上电抽测 ⏸️

**状态**：等实物，不阻塞 M1~M3 软件设计

**说明**：主板到货后再做，作为 M1 进入"真实硬件联调"的强前置。

## 下一步行动

### ~~立即执行（P0）~~ ✅ 已完成

1. ~~**修复 U8 GPIO 配置错误**~~ ✅
   - 经 PADS Logic `.txt` 核对，GPIO38/39/40 分别连接 `IO38_I2S_MCK`、`PA_EN`、`DVP_PWDN`
   - 已更新检查工具，不再把 IO38/39/40 标记为弱证或错误

2. ~~**标注 U1 Strapping Pin 风险**~~ ✅
   - U1 配置文件已有"已知风险"标注
   - 已改进检查工具，正确识别下一行注释中的标注

3. ~~**实现 M0c.1 fake U1**~~ ✅
   - 创建 `tools/fake_u1/` 目录
   - 实现 TCP server 与完整状态机
   - 支持 GET_STATUS/HOME/MOVE/PATH_*/PAUSE/RESUME/STOP/ESTOP
   - 支持错误注入（E001/E005/E006/E008）
   - 13/13 单元测试通过

### 短期计划（P1）

4. **完善 M0d CI**
   - ✅ 实现 schema 校验脚本
   - ✅ 集成 fake 环境测试
   - ⏳ 配置 GitHub required checks

### 中期计划（P2）

5. **实现 M0c fake DeviceServer 和 fake AI**
6. **准备 M1 开发环境**

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| GPIO 配置错误导致硬件损坏 | 中 | 高 | ✅ M0b 工具已实现，可提前发现 |
| 主板到货延迟 | 中 | 中 | ✅ M1~M3 对着 fake 开发，不阻塞 |
| Strapping pin 影响启动 | 低 | 中 | ⚠️ 需要标注风险并测试 |

## 参考文档

- 实施计划 v2：`docs/实施计划-v2.md`
- 架构定稿 v2：`docs/架构定稿-v2.md`
- 硬件文档：`docs/硬件连接与GPIO分配说明.md`
- Superpowers 原则：`.cursor/rules/superpowers-and-context.mdc`

## 修订记录

- 2026-05-14：初始版本，完成 M0a、M0b、M0e
- 2026-05-14 (下午)：M0b 问题修复完成，所有检查通过
- 2026-05-14 (晚上)：M0c.1 fake U1 完成，13/13 测试通过
- 2026-05-14 (晚上)：纳入 PADS `.txt` 硬件源文件，GPIO 检查更新为 0 错误、0 警告、0 信息
- 2026-05-14 (晚上)：按 SCH PDF/BOM 确认四路步进驱动实际型号为 `HR4988E`；旧 `TMC2208-*` 作为网络名保留，固件 GPIO 无需调整
