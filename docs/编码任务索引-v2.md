# 编码任务索引 v2

本文件把 [实施计划-v2.md](./实施计划-v2.md) 的 `M1 ~ M6` 子步骤映射到当前仓库的真实目录、主要文件、输入输出和验收命令。

用途：

- 把计划直接转成可派发的编码任务
- 降低“知道要做什么，但不知道改哪里”的切换成本
- 让每个子步都能独立验收

约束：

- 以 `docs/架构定稿-v2.md` 为语义基线
- 以 `docs/实施计划-v2.md` 为顺序基线
- 若文档与代码现实不符，先改文档，再改实现

---

## 0. 仓库角色映射

| 角色 | 目录 | 说明 |
|---|---|---|
| `U1` | `firmware/u1-grbl/Grbl_Esp32/` | 运动执行、私有协议解析、安全裁决 |
| `U8` | `firmware/u8-xiaozhi/` | 设备侧 AI MCU、U1 协议桥 |
| `BusinessServer` | `server/xiaozhi-esp32-server/main/manager-api/` | 账号、任务、资源、授权、持久化 |
| `DeviceServer` | `server/xiaozhi-esp32-server/main/xiaozhi-server/` | 设备会话、语音链路、任务转发 |
| `Client` | `server/xiaozhi-esp32-server/main/manager-mobile/` | 微信小程序 |

---

## M1 设备协议端到端

### M1.0 Edge-D 固件静态契约收口

- 状态：已完成（2026-05-15，无主板静态验证）
- 目录：
  - `firmware/u8-xiaozhi/main/boards/zhuguang/dlc-motor-control-p1-ai/`
  - `firmware/u1-grbl/Grbl_Esp32/src/`
  - `tests/ci/`
- 主要文件：
  - `dlc_motor_control_p1_ai_board.cc`
  - `Protocol.cpp`
  - `tests/ci/test_edge_d_firmware_static.py`
  - `docs/M1-edge-d-static-contract-plan.md`
- 输入：
  - Edge-D `cmd/ack/device_info/error` schema
  - U8 生成的 `@{json}` command frame
  - U1 `ack/device_info/error` response literals
- 输出：
  - U8 command 不再带 `type:"cmd"`
  - U1 未支持/无效错误统一落到当前 schema 已声明的 `E009`
  - U1 ack 统一带 `accepted:true`
  - U1 `GET_DEVICE_INFO` 返回 schema-locked `model / hw_rev / fw_rev / workspace_mm`
  - U8 `self.motor.get_device_info`、motion_task `get_device_info` 和 response passthrough 已静态落地
- 验收命令：
  - `rtk python -m unittest tests.ci.test_edge_d_firmware_static -v`
  - `rtk python -m unittest discover -s tests -p "test_*.py" -v`
  - `rtk powershell.exe -NoProfile -Command "& 'C:\Users\zhugu\.platformio\penv\Scripts\platformio.exe' run -e release_esp32s3"`（工作目录：`firmware/u1-grbl`）
  - U8 ESP-IDF v5.5.4 / esp32s3 构建已通过；实机刷写与联调待补

### M1.1 固化 Edge-D 最小样例集

- 状态：已完成（2026-05-14）

- 目录：`docs/schemas/edge_d/`
- 主要文件：
  - `docs/schemas/edge_d/examples/*.json`
  - `docs/schemas/README.md`
- 输入：
  - `GET_STATUS / HOME / MOVE / ERROR` 样例帧
- 输出：
  - 可复用的协议样例集
- 验收命令：
  - `rtk python -m json.tool docs/schemas/edge_d/examples/get_status.request.json`

### M1.2 `homed` 字段说真话
- 状态：已核实满足（2026-05-14，当前仓库已使用 `sys.is_homed`）

- 状态：已完成
- 目录：`firmware/u1-grbl/Grbl_Esp32/src/`
- 主要文件：
  - `Report.cpp`
  - `MotionControl.cpp`
  - `Grbl.cpp`
  - `System.h`
- 输入：
  - Grbl 真实归零状态
- 输出：
  - `GET_STATUS.homed`
- 实现备注：
  - 新增 `sys.is_homed`
  - 上电/复位后按 `homing_enable` 初始化
  - homing 成功后置真，未完成或中断保持/回到假
- 验收命令：
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/Report.cpp' -Pattern 'is_homed|\"homed\"'`
  - `& 'C:\Users\zhugu\.platformio\penv\Scripts\platformio.exe' run -e release_esp32s3`

### M1.3 `alarm_code / error_code` 区分

- 状态：已核实满足（2026-05-14，`alarm_code` 已按 `ExecAlarm` 映射，`error_code` 在 status 中保持 `null`）
- 收口（2026-05-14）：`Report.cpp:857-859` 三元同值表达式化简为常量 + 契约注释，已通过 `release_esp32s3` 编译验证
- 状态：已完成
- 目录：`firmware/u1-grbl/Grbl_Esp32/src/`
- 主要文件：
  - `Report.cpp`
  - `Error.h`
  - `System.*`
- 输入：
  - `sys.state`
  - `sys_rt_exec_alarm`
- 输出：
  - 正确的 `alarm_code` / `error_code`
- 实现备注：
  - `HardLimit -> E005`
  - `SoftLimit -> E002`
  - `HomingFail* -> E006`
  - `AbortCycle -> E007`
  - 其他未细分告警先归 `E009`
  - `status` 帧中的 `error_code` 不再把全部 `ALARM` 误报成 `E006`
  - `status` 帧 `error_code` 字段恒为 `null`，错误码走独立 `error` 帧（契约写在源码注释，待 M0a 同步到 schema）
- 验收命令：
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/Report.cpp' -Pattern 'E005|E002|E006|E007|E009'`
  - `& 'C:\Users\zhugu\.platformio\penv\Scripts\platformio.exe' run -e release_esp32s3`

### M1.4 固定 `STOP / ESTOP` 真实语义

- 状态：已核实满足（2026-05-14，v2 文档已固定语义边界）

- 目录：`docs/`
- 主要文件：
  - `架构定稿-v2.md`
  - `实施计划-v2.md`
- 输入：
  - 当前状态机语义
- 输出：
  - 硬约束文档
- 验收命令：
  - `rtk rg "STOP|ESTOP|cancelled|E_ESTOP|E_ABORTED" docs`

### M1.5 U1 实现 `PAUSE / RESUME / STOP / ESTOP`

- 状态：已完成（2026-05-14，本轮补齐 `ESTOP ack` 与告警持久化）
- 状态：已完成
- 目录：`firmware/u1-grbl/Grbl_Esp32/src/`
- 主要文件：
  - `Protocol.cpp`
  - `Exec.cpp`
  - `Exec.h`
  - `MotionControl.cpp`
  - `Report.cpp`
- 输入：
  - 私有协议命令帧
- 输出：
  - `ack / error / state transition`
- 实现备注：
  - `PAUSE -> feedHold`
  - `RESUME -> cycleStart`
  - `STOP -> 先 hold，hold 完成后通过 `motionCancel + cycleStart` 收敛到 `IDLE``
  - `ESTOP -> EmergencyStop + mc_reset`
  - 新增 `ExecAlarm::EmergencyStop -> E008`
- 验收命令：
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/Protocol.cpp' -Pattern 'PAUSE|RESUME|STOP|ESTOP|private_stop_requested'`
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/Report.cpp' -Pattern 'E008|EmergencyStop'`
  - `& 'C:\Users\zhugu\.platformio\penv\Scripts\platformio.exe' run -e release_esp32s3`

### M1.6 `at_home` 派生位补全

- 状态：已完成
- 目录：`firmware/u1-grbl/Grbl_Esp32/src/`
- 主要文件：
  - `Report.cpp`
  - `SettingsDefinitions.cpp`
  - `Protocol.cpp`
- 输入：
  - `position`
  - `home_position`
- 输出：
  - 供上层派生 `at_home` 的状态字段
- 实现备注：
  - U1 不直接输出 `at_home`
  - `GET_STATUS` 持续输出真实 `homed + position`
  - `Home/Mpos` 轴配置仍由设置层提供给上层派生参考
  - 同步修正 `active_task_id`，避免状态帧误回显当前查询的 `task_id`
- 验收命令：
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/SettingsDefinitions.cpp' -Pattern 'Home/Mpos'`
  - `Select-String -Path 'firmware/u1-grbl/Grbl_Esp32/src/Report.cpp' -Pattern 'homed|position|active_task_id'`
  - `& 'C:\Users\zhugu\.platformio\penv\Scripts\platformio.exe' run -e release_esp32s3`

### M1.7 删除 U8 G-code 回退路径

- 状态：已完成静态收口（2026-05-15）；ESP-IDF v5.5.4 / esp32s3 构建已通过，`sdkconfig.defaults` 板型与 HTTPD WebSocket 默认项已有静态测试守护
- 目录：`firmware/u8-xiaozhi/main/boards/zhuguang/dlc-motor-control-p1-ai/`
- 主要文件：
  - `dlc_motor_control_p1_ai_board.cc`
  - `config.h`
- 输入：
  - `run_path` capability
- 输出：
  - 仅走私有协议，不再回退直推 G-code
- 验收命令：
  - `rtk rg "LoadJob|StartJob|\\$J=G91|M3 S|send_gcode" firmware/u8-xiaozhi`

### M1.8 U8 编码层最小闭环

- 状态：已完成（静态核对；ESP-IDF v5.5.4 / esp32s3 构建已通过）
- 目录：`firmware/u8-xiaozhi/main/boards/zhuguang/dlc-motor-control-p1-ai/`
- 主要文件：
  - `dlc_motor_control_p1_ai_board.cc`
  - `config.h`
- 输入：
  - `GET_STATUS / HOME / MOVE`
- 输出：
  - 编码后的 `@{json}\n`
- 验收命令：
  - `rtk rg "ExecuteHomeCapability|ExecuteGetStatusCapability|ExecuteMoveCapability|ParseCapabilityResponse" firmware/u8-xiaozhi`

### M1.9 fake U1 回归

- 状态：已完成（2026-05-15，schema 回归测试已覆盖 `status/result/error`）
- 目录：`tools/fake_u1/`
- 主要文件：
  - `tools/fake_u1/app.py`
  - `tools/fake_u1/tests/`
- 输入：
  - 私有协议帧
- 输出：
  - `ack/status/result/error`
- 验收命令：
  - `rtk python tools/fake_u1/app.py --help`
  - `rtk python -m unittest tools.fake_u1.tests.test_app -v`

### M1.10 U8 ↔ U1 最小真链路

- 目录：
  - `firmware/u8-xiaozhi/`
  - `firmware/u1-grbl/`
- 主要文件：
  - `dlc_motor_control_p1_ai_board.cc`
  - `Protocol.cpp`
  - `Report.cpp`
- 输入：
  - 3 条 capability
- 输出：
  - 能重复跑通的最小协议链
- 验收命令：
  - `rtk rg "GET_STATUS|HOME|MOVE" firmware/u8-xiaozhi firmware/u1-grbl/Grbl_Esp32/src`

---

## M2 小程序订阅链路

### M2.1 最小数据库初始化

- 状态：已完成静态落地（Liquibase 变更集已追加；当前环境缺少 `mvn/mvnw`）
- 目录：`server/xiaozhi-esp32-server/main/manager-api/`
- 主要文件：
  - `src/main/resources/db/changelog/`
- 输入：
  - `accounts / devices / device_bindings / activation_codes / tasks`
- 输出：
  - 最小业务表
- 验收命令：
  - `rtk mvn -q test`
  - `rtk rg "accounts|devices|device_bindings|activation_codes|tasks" server/xiaozhi-esp32-server/main/manager-api/src/main/resources/db/changelog/202605141200.sql`

### M2.2 REST 三件套

- 状态：代码已落地；§3.9 / §3.10 两条红线已修（2026-05-14）；**待补**：本机无 mvn，未跑 `rtk mvn test`，且 §3.11 JWT primary_session_id 留作 M6.4
- 目录：`manager-api/src/main/`
- 主要文件：
  - `controller/`
  - `service/`
  - `dto/`
  - `test/`
- 输入：
  - 登录、绑定、下发任务请求
- 输出：
  - `JWT`
  - `device binding`
  - `task_id`
- 验收命令：
  - `rtk mvn test`
  - `rtk rg "/api/v1/login|/api/v1/devices/bind|/api/v1/devices/.*/tasks" server/xiaozhi-esp32-server/main/manager-api/src/main/java`
- 已收口：
  - 安全漏洞 1（§3.9）：login 改为 `code → WechatLoginGateway.exchange(code) → unionid`；dev 走 `v2.wechat.miniapp.mock-mode=true`，生产必须填 `app-id/app-secret`
  - 安全漏洞 2（§3.10）：账号级 `SecureRandom` 随机密码取代 `V2Temp!Pass1`
- 阻塞项：
  - JWT 载荷 `primary_session_id` 字段缺失，留 M6.4 主控仲裁统一处理（不构成红线）

### M2.3 Edge-B 最小 `motion_task` 下发

- 状态：已完成（2026-05-14，BusinessServer → DeviceServer HTTP + Bearer；dev 默认不启用转发）

- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
- 主要文件：
  - `manager-api/.../V2DeviceServerProperties.java`
  - `manager-api/.../DeviceServerMotionGateway*.java`
  - `manager-api/.../AppV2ServiceImpl.java`
  - `manager-api/.../application-dev.yml`（`v2.device-server`）
  - `xiaozhi-server/core/api/motion_task_handler.py`（HTTP 入口）
  - `xiaozhi-server/config.yaml`（`server.internal_motion_task_token`）
- 输入：
  - `motion_task`
- 输出：
  - DeviceServer 收到任务并打 log；可选转发 WSS（M2.4）
- 验收命令：
  - `rtk rg "internal/v1/motion_task|DeviceServerMotionGateway|v2.device-server" server/xiaozhi-esp32-server/main/manager-api`
  - `rtk rg "motion_task_handler|internal_motion_task_token" server/xiaozhi-esp32-server/main/xiaozhi-server`

### M2.4 DeviceServer 转发到 U8

- 状态：已完成 DeviceServer 侧（2026-05-14）；U8 消费 WSS 帧留 **M2.5**

- 目录：`server/xiaozhi-esp32-server/main/xiaozhi-server/core/`
- 主要文件：
  - `handle/motionHandle.py`（`build_motion_task_websocket_message`）
  - `api/motion_task_handler.py`（HTTP → WSS）
  - `websocket_server.py`（`device_id` 会话表、`send_motion_task_to_device`）
  - `connection.py`（注册/注销会话）
  - `http_server.py`、`app.py`（注入 `WebSocketServer`）
- 输入：
  - HTTP JSON（与 M2.3 一致）
- 输出：
  - 发给设备的 WSS 文本帧 `type=motion_task`
- 验收命令：
  - `rtk rg "motion_task|send_motion_task_to_device|attach_motion_device" server/xiaozhi-esp32-server/main/xiaozhi-server`

### M2.5 U8 接 `motion_task`

- 状态（2026-05-14）：已接入（云端 WSS JSON 经 `Application`，非板载 8080 MCP WS）。
- 目录：`firmware/u8-xiaozhi/`
- 主要文件：
  - `main/application.cc`（`type=motion_task` 分发）
  - `main/boards/common/board.h`、`board.cc`（`HandleMotionTaskJson` 默认空实现）
  - `main/boards/zhuguang/dlc-motor-control-p1-ai/dlc_motor_control_p1_ai_board.cc`（capability → U1）
- 输入：
  - WSS `motion_task`
- 输出：
  - U1 私有协议帧
- 验收命令：
  - `rtk rg "motion_task|HandleMotionTaskJson|capability" firmware/u8-xiaozhi`

### M2.6 反向 `motion_event` 最小链路

- 状态（2026-05-14）：已实现（固件相位事件 → WSS `motion_event` → xiaozhi-server HTTP 转发 → manager-api 内网入口日志）。
- 目录：
  - `firmware/u8-xiaozhi/`
  - `server/xiaozhi-esp32-server/main/xiaozhi-server/`
  - `server/xiaozhi-esp32-server/main/manager-api/`
- 主要文件：
  - `main/protocols/protocol.cc`（`SendMotionEvent`）、`main/application.cc`、`dlc_motor_control_p1_ai_board.cc`（`EmitMotionEventPhase` 等）
  - `core/handle/textHandler/motionEventMessageHandler.py`、`textMessageHandlerRegistry.py`、`config.yaml`
  - `InternalMotionEventController.java`、`AppV2ServiceImpl.ingestMotionEvent`、`ShiroConfig.java`
- 契约：`docs/schemas/edge_c/README.md`、`docs/schemas/edge_c/examples/motion_event.minimal.uplink.json`
- 输入：
  - U1 执行结果（经板卡解析为 `done`/`failed`）
- 输出：
  - WSS JSON `type=motion_event`；HTTP `POST .../internal/v1/motion_event`
- 验收命令：
  - `rtk rg "motion_event|SendMotionEvent|motion_event_business" firmware/u8-xiaozhi server/xiaozhi-esp32-server/main`

### M2.7 Edge-A 最小订阅

- 状态（2026-05-14）：已完成（见实施计划 M2.7 文件清单）。
- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 主要文件：
  - `xiaozhi/modules/appv2/ws/ClientWebSocketConfig.java`、`ClientEdgeWebSocketHandler.java`、`EdgeAClientHub.java`
  - `ShiroConfig.java`（`/ws/v1/client` anon）
  - `AppV2ServiceImpl.ingestMotionEvent`（广播）
  - `manager-mobile/src/utils/index.ts`（`buildEdgeAClientWsUrl`）
  - `manager-mobile/src/pages/device/index.vue`（调试区）
- 契约：`docs/schemas/edge_a/README.md`、`docs/schemas/edge_a/examples/`
- 输入：
  - `device_id`（已绑定设备）、登录 JWT
- 输出：
  - WSS 实时 `type=event`（由 `motion_event` ingest 触发）
- 验收命令：
  - `rtk rg "ws/v1/client|EdgeAClientHub|buildEdgeAClientWsUrl" server/xiaozhi-esp32-server/main/manager-api server/xiaozhi-esp32-server/main/manager-mobile`
  - `rtk corepack pnpm -C server/xiaozhi-esp32-server/main/manager-mobile lint`

### M2.8 小程序最小页面

- 状态：设计期页面链路持续可构建（2026-05-15，`pnpm-lock.yaml` 已补齐 `sm-crypto`，`rtk corepack pnpm run type-check` 与 `rtk corepack pnpm run build:mp-weixin` 均通过；Wot Design Uni API import 已从根入口收敛到子模块入口，避免 `vue-tsc` 检查无关第三方源码；CI 已新增 `manager-mobile-tests` job，在 clean checkout 上守护 frozen lockfile、type-check 与微信小程序构建）
- 目录：`server/xiaozhi-esp32-server/main/manager-mobile/src/`
- 主要文件：
  - `pages/`
  - `api/`
  - `store/`
- 输入：
  - 登录
  - 绑定
  - 点“归零”
- 输出：
  - 设备详情页状态变化
- 验收命令：
  - `rtk corepack pnpm run build:mp-weixin`（工作目录：`server/xiaozhi-esp32-server/main/manager-mobile`）
  - `rtk corepack pnpm run type-check`（工作目录：`server/xiaozhi-esp32-server/main/manager-mobile`）

### M2.9 五态落库

- 状态：设计期已完成（2026-05-15，`AppV2ServiceImpl.ingestMotionEvent` 会更新 `V2TaskEntity.status / started_at / finished_at / error_code / error_message / result_json`；`rejected` 归入失败终态；`AppV2ServiceImplTest` 已覆盖 running、done、failed、rejected、result_json 与 device_info 不触碰任务状态）
- 目录：`manager-api/`
- 主要文件：
  - `AppV2ServiceImpl`
  - `V2TaskDao`
  - `V2TaskEntity`
- 输入：
  - `motion_event`
- 输出：
  - 持久化任务状态
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=AppV2ServiceImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M2.10 `seq` 单调递增

- 状态：设计期已完成内存版（2026-05-15，`EdgeAClientHub` 按 `device_id` 维护单调 `seq`，Edge-A 事件均带 `seq`，按 topic 缓存最近 200 条；`ClientEdgeWebSocketHandler` 支持 `since_seq` 重放和 `ack` 单调确认；持久化 seq 留到 M2b/生产化）
- 目录：`manager-api/`
- 主要文件：
  - `EdgeAClientHub`
  - `ClientEdgeWebSocketHandler`
- 输入：
  - per-device event stream
- 输出：
  - `seq`
- 验收命令：
  - `rtk rg "since_seq|seq" server/xiaozhi-esp32-server/main/manager-api`
  - `rtk powershell.exe -Command "mvn '-Dtest=EdgeAClientHubTest,ClientEdgeWebSocketHandlerTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M2.11 幂等提交

- 状态：设计期已完成（2026-05-15，`submitTask` 在 `(account_id, device_id, request_id)` 命中既有任务时返回原 `task_id/status`，不重复转发 DeviceServer；新任务会规范化 `device_id/request_id/trace_id/capability/source` 后写入并转发；小程序 `v2SubmitTask` 默认生成 `client-<capability>-...` requestId，所有任务入口都会带幂等键）
- 目录：`manager-api/`
- 主要文件：
  - `AppV2ServiceImpl.submitTask`
  - `V2TaskDao`
- 输入：
  - `(account, device, request_id)`
- 输出：
  - 唯一 `task_id`
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=AppV2ServiceImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M2.12 `run_path` 端到端

- 状态：设计期最小链路已补齐（2026-05-15，U8 `run_path` 每个 `PATH_SEG` ACK 后发 `motion_event phase=progress`，携带 `progress.done_segments / total_segments / percent`；`PATH_END` 后发 `done/failed`。Edge-A/B/C schema 已允许 `progress`；BusinessServer 把 `progress` 归一为 running 并透传 Edge-A payload；小程序设备详情页显示进度条。fake DeviceServer 已把 `run_path` motion_task 展开成 `PATH_BEGIN/PATH_SEG/PATH_END`，HTTP 集成测试可驱动 fake U1 更新位置。U8 ESP-IDF v5.5.4 / esp32s3 构建已通过；U1 实机执行、U8 刷写与联调待硬件环境补验）
- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - `u8-xiaozhi/`
  - `u1-grbl/`
- 输入：
  - 小路径方框
- 输出：
  - `progress`
  - `done`
- 验收命令：
  - `rtk python -m unittest tools.fake_device_server.tests.test_app tests.ci.test_fake_integration -v`
  - `rtk python -m unittest tests.ci.test_edge_d_firmware_static tests.ci.test_manager_mobile_device_info tests.ci.test_xiaozhi_server_motion_event_tts_hint -v`
  - `rtk python tools/validate_schemas.py`
  - `rtk powershell.exe -Command "mvn '-Dtest=AppV2ServiceImplTest,EdgeAClientHubTest,ClientEdgeWebSocketHandlerTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）
  - `rtk powershell.exe -ExecutionPolicy Bypass -Command ". 'C:\Users\zhugu\Desktop\xue\esp-idf-v5.5.4\export.ps1'; idf.py set-target esp32s3; idf.py build"`（工作目录：`firmware/u8-xiaozhi`；2026-05-15 已通过）

### M2.13 `get_device_info` 最小链路

- 状态：部分完成（2026-05-15，Edge-D/Edge-C/Edge-A schema、fake U1、fake DeviceServer HTTP 转发、真实 DeviceServer `type=device_info` 上行 handler、U1、U8 静态链路已落；U8 已在 `GET_DEVICE_INFO` 成功后补发单独 `type=device_info` 帧；BusinessServer `/internal/v1/device_info` 最小 ingest 已落地，会更新 `devices` 能力快照并广播 Edge-A `device_info_reply`；小程序 UI 已能提交 `get_device_info`、接收并展示 `device_info_reply`；真实 DeviceServer 已将 `device_info_reply` 摘要入队 TTS，语音自然语言意图映射和实机播放待 M2.14/M2.15 验证）
- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - `u8-xiaozhi/`
  - `u1-grbl/`
- 输入：
  - `get_device_info`
- 输出：
  - `model / hw_rev / fw_rev / workspace_mm`
- 验收命令：
  - `rtk rg "get_device_info|GET_DEVICE_INFO|workspace_mm" server firmware`
  - `rtk python -m unittest tests.ci.test_fake_integration -v`
  - `rtk python -m unittest tests.ci.test_xiaozhi_server_device_info -v`
  - `rtk python -m unittest tests.ci.test_manager_mobile_device_info -v`
  - `rtk powershell.exe -NoProfile -Command "mvn '-Dtest=AppV2ServiceImplTest,InternalMotionEventControllerTest,EdgeAClientHubTest,DeviceServerMotionGatewayImplTest,ClientEdgeWebSocketHandlerTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M2.14 语音安全动作白名单

- 状态：设计期已补齐（2026-05-15，U8 已新增 `self.motor.pause/resume/stop` 设备 MCP 工具和 motion_task capability 分支；真实 xiaozhi-server 已新增 `型号/设备信息 -> self.motor.get_device_info`、`归零/回原点 -> self.motor.home`、`暂停 -> self.motor.pause`、`继续/恢复 -> self.motor.resume`、`停止/停下 -> self.motor.stop` 的确定性映射，且仅在对应工具已注册时生效；U8 已新增 `self.motor.move_rel` 小步白名单，语音只映射“往左/右/前/后/上/下/抬高/降低一点”等明确相对移动短语，每轴限制 `-1/0/1mm`，执行前读取 `GET_STATUS` 当前位置和 `GET_DEVICE_INFO` 工作空间，超步长、零步长、越界或无法验证工作空间时拒绝；U8 ESP-IDF v5.5.4 / esp32s3 构建已通过，实机动作仍待硬件环境补验）
- 目录：
  - `xiaozhi-server/`
  - `manager-api/`
- 输入：
  - `voice intent: home / move_abs / pause / resume / stop / get_device_info`
- 输出：
  - 受限动作映射与越界拒绝
- 验收命令：
  - `rtk rg "intent_submit|move_abs|get_device_info|voice" server/xiaozhi-esp32-server/main`
  - `rtk python -m unittest tests.ci.test_xiaozhi_server_device_intent_map -v`

### M2.15 任务开始/结束语音播报协议

- 状态：设计期最小链路已补齐（2026-05-15，U8 `motion_event` 透传 `source=voice|client`；xiaozhi-server 按 `accepted/done/failed/rejected` 生成并入队 `tts_hint.tts_event=task_started|task_done|task_failed|task_rejected`，`running/progress` 不播报；`device_info` 生成 `device_info_reply` TTS hint；BusinessServer Edge-A 广播保留 `job_status.payload.tts_hint` 和 `device_info_reply.payload.tts_hint`；实机 TTS 播放待硬件环境补验）
- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - `manager-mobile/`
- 输入：
  - `job_accepted / job_done / job_error`
  - `source=voice|client`
  - `get_device_info`
- 输出：
  - `tts_hint.tts_event = task_started|task_done|task_failed|task_rejected|device_info_reply`
- 验收命令：
  - `rtk rg "tts_hint|task_started|task_done|task_failed|device_info_reply" server/xiaozhi-esp32-server/main`

---

## M3 投影管线

### M3.1 资源域数据库补表

- 状态：设计期数据库层已落地（2026-05-15）。`manager-api/src/main/resources/db/changelog/202605151610.sql` 已注册到 `db.changelog-master.yaml`，创建 `fonts / copybooks / assets / ai_plans / entitlements`，并覆盖资源主键、状态、价格模型、显式素材 `fallback_only`、默认 fake AI plan、授权来源、过期时间、账号-资源唯一键与资源查询索引。`tests/ci/test_manager_api_resource_domain.py` 已静态守护 changelog、字段、索引、seed 与消费侧授权边界；`ResourceEntitlementService` 已在提交前接入 `write_text` 字体授权、显式 starter asset 授权和 prompt 生成默认 `ai_plan:local_fake_ai_plan_v1` 授权校验，小程序已把 `E_NOT_ENTITLED` 映射为资源未开通提示，真实 AI provider/真实 AI plan 授权仍待后续接入。
- 目录：`manager-api/src/main/resources/db/migration/`
- 主要文件：
  - migration SQL
- 输入：
  - `fonts / copybooks / ai_plans / assets(explicit_only) / entitlements`
- 输出：
  - 资源域表
- 验收命令：
  - `./mvnw test`

### M3.2 出厂默认资源装载

- 状态：设计期 seed 与运行时注入已落地（2026-05-15）。`202605151610.sql` 幂等初始化 `kai_basic_v1`、`pinyin_basic_v1`、5 件显式 starter 素材 `starter_star/starter_house/starter_tree/starter_fish/starter_flower` 与默认 fake AI plan `local_fake_ai_plan_v1`；静态测试确认 seed 与 `StarterAssetCatalog` 保持一致。`FactoryEntitlementService.ensureFactoryEntitlements` 会在 `bindDevice` 新绑定和同账号幂等重绑成功路径中补齐 `entitlements(source=factory)`，并按账号-资源查重避免重复插入；`ResourceEntitlementService` 已消费这些 entitlement，提交 `write_text` 时校验字体授权，显式 starter 链路校验 asset 授权，prompt 生成链路校验默认 AI plan 授权。
- 目录：`manager-api/`
- 主要文件：
  - seed/init service
- 输入：
  - 默认字体、默认预置素材（仅显式点选可用）
- 输出：
  - 免费字体集合 + 预置素材集合
- 验收命令：
  - `rtk rg "kai_basic_v1|starter_" server/xiaozhi-esp32-server/main/manager-api`

### M3.3 字体引擎接入

- 目录：`manager-api/src/main/`
- 主要文件：
  - `service/glyph/`
- 输入：
  - `text`
  - `font`
- 输出：
  - glyph path
- 验收命令：
  - `./mvnw test`

### M3.4 投影管线四阶

- 目录：`manager-api/src/main/`
- 主要文件：
  - `service/projection/WriteTextProjectionService.java`
  - `service/projection/RunPathProjection.java`
  - `service/projection/PathPoint.java`
  - `src/test/java/xiaozhi/modules/appv2/service/projection/WriteTextProjectionServiceTest.java`
- 输入：
  - `write_text`
- 输出：
  - `segments`
- 验收命令：
  - `rtk rg "discretize|canvas_transform|segments" server/xiaozhi-esp32-server/main/manager-api`

### M3.5 安全裁决前置

- 目录：`manager-api/src/main/`
- 主要文件：
  - `service/safety/SafetyValidator.java`
  - `service/safety/DeviceCaps.java`
  - `service/safety/DeviceRuntimeState.java`
  - `service/safety/PathBounds.java`
  - `service/safety/SafetyDecision.java`
  - `src/test/java/xiaozhi/modules/appv2/service/safety/SafetyValidatorTest.java`
- 输入：
  - device caps
  - runtime state
  - path bounds
- 当前状态：设计期最小 validator 已落地，可无硬件验证 `E_NOT_HOMED / E_OUT_OF_RANGE / E_DEVICE_BUSY / E_ESTOP`，并覆盖 `feed_rate <= max_feed_rate` 的 `E_INVALID_PARAM`；`AppV2ServiceImpl.submitTask` 已在落库和 DeviceServer 转发前对真实 dispatch 请求执行投影后 `run_path/move_abs` 几何与 feed 前置裁决，拒绝时不 insert、不 forward；`motion_event.result` 或顶层携带 `state/homed/position` 时会更新 `devices.runtime_state/homed/position_mm/runtime_seen_at`，安全裁决会优先使用已缓存 runtime；runtime 快照超过 5s 会以 `E_RUNTIME_STALE` 前置拒绝并写入 `safety_audit`，同时尝试通过 DeviceServer 下发内部 `get_status` 刷新请求，刷新请求不落库、不携带 `source`，避免触发客户端/语音任务语义；BusinessServer 安全拒绝会通过 `SafetyAuditService(REQUIRES_NEW)` 写入 `safety_audit`；U1 上报的 `motion_event.error_code` 会按任务上下文镜像写入 `safety_audit`；`SafetyAuditRetentionTask` 每日清理超过 180 天的审计记录；客户端收到 `E_RUNTIME_STALE` 后会提示“设备状态正在刷新，请稍后重试”
- 输出：
  - allow / deny
- 验收命令：
    - `rtk powershell.exe -Command "mvn '-Dtest=SafetyValidatorTest,SafetyAuditServiceTest,AppV2ServiceImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M3.6 `write_text` demo

- 状态：设计期最小链路已落地（2026-05-15）。`write_text("你好")` 会在 BusinessServer 内投影为 `run_path`，并经 Edge-B motion_task 以 `capability=run_path`、`params.feed/path` 下发；任务落库仍保留原始 `write_text` capability 和原始 params。小程序设备详情页已提供最小写字输入框和“开始写字”按钮，提交 `write_text` 时携带 `font_id=kai_basic_v1`，并复用现有 WSS 进度和友好错误提示。当前使用占位单线 glyph，不代表完整楷体字形；真实字体引擎和实机写出仍待后续补齐。
- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 输入：
  - `"你好"`
- 输出：
  - 可执行 `run_path`
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=WriteTextProjectionServiceTest,AppV2ServiceImplTest,DeviceServerMotionGatewayImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M3.7 内容审核接入

- 状态：设计期最小本地层已落地（2026-05-15）。`ContentAuditService` 支持本地关键词入站审核；`AppV2ServiceImpl.submitTask` 对 `write_text.params.text` 和 `draw_generated.params.prompt/svg_text/transcript` 在落库/投影/下发前审核，命中“不当文字”等关键词时返回 `E_CONTENT_BLOCKED`，并保证不 insert、不 forward。被拦截内容会通过 `ContentAuditLogService(REQUIRES_NEW)` 写入 `content_audit(account_id, device_id, path, raw_hash, rule_hit, ts)`，只保留 SHA-256 哈希、实际字段路径和规则命中，不落原文；`ContentAuditRetentionTask` 每日清理超过 180 天的审计记录；`tests/ci/test_manager_api_content_audit.py` 已静态守护 changelog、“不落原文”和 `draw_generated` 文本输入审核约束。小程序设备详情页已把 `E_CONTENT_BLOCKED` 映射为儿童友好提示；云端语义审核尚未接入。
- 目录：`manager-api/src/main/`
- 主要文件：
  - `service/contentaudit/ContentAuditService.java`
  - `service/contentaudit/ContentAuditException.java`
  - `service/contentaudit/ContentAuditLogService.java`
  - `service/contentaudit/ContentAuditRetentionTask.java`
  - `dao/V2ContentAuditDao.java`
  - `entity/V2ContentAuditEntity.java`
  - `resources/db/changelog/202605151545.sql`
  - `src/test/java/xiaozhi/modules/appv2/service/contentaudit/ContentAuditServiceTest.java`
  - `src/test/java/xiaozhi/modules/appv2/service/contentaudit/ContentAuditLogServiceTest.java`
  - `src/test/java/xiaozhi/modules/appv2/service/AppV2ServiceImplTest.java`
  - `tests/ci/test_manager_api_content_audit.py`
  - `server/xiaozhi-esp32-server/main/manager-mobile/src/pages/v2/device-detail/index.vue`
  - `tests/ci/test_manager_mobile_device_info.py`
- 输入：
  - text / draw request
- 输出：
  - pass / `E_CONTENT_BLOCKED`
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=ContentAuditServiceTest,ContentAuditLogServiceTest,AppV2ServiceImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）
  - `rtk python -m unittest tests.ci.test_manager_api_content_audit -v`
  - `rtk python -m unittest tests.ci.test_manager_mobile_device_info -v`
  - `rtk corepack pnpm run type-check`（工作目录：`server/xiaozhi-esp32-server/main/manager-mobile`）

### M3.8 `draw_generated` 生成约束校验

- 状态：设计期最小 SVG / bitmap 约束已落地（2026-05-15）。`SingleLineSvgValidator` 只接受单条黑色 `path` 且 `fill=none`，拒绝填充形状、多 path、非黑色 stroke、空 path 和超节点数；`draw_generated.params.svg_text` 在落库/转发前校验，非法时返回 `E_INVALID_DRAWING`，并保证不 insert、不 forward。`DrawGeneratedProjectionService` 已补总长度与预计耗时上限（设计期默认：总长度 ≤ 5000mm、预计耗时 ≤ 600s），并支持 `bitmap_base64 / bitmap_data_uri / image_base64` 最小二值化/暗像素扫描线矢量化，空白图、过大图、过复杂图会稳定返回 `E_INVALID_DRAWING` 分类。`AppV2ServiceImpl` 会在投影前按设备 `workspace_mm` 扣除默认 `safe_margin_mm` 注入/裁剪 `canvas_width_mm / canvas_height_mm / origin_x_mm / origin_y_mm`，让生成路径默认落入可写区。小程序设备详情页已把 `E_INVALID_DRAWING` 映射为友好提示；真实 AI 重生成、生产级图片矢量化、完整尺寸指令解析仍未接入。
- 目录：`manager-api/src/main/`
- 主要文件：
  - `service/graphic/SingleLineSvgValidator.java`
  - `service/graphic/DrawingValidationException.java`
  - `src/test/java/xiaozhi/modules/appv2/service/graphic/SingleLineSvgValidatorTest.java`
  - `src/test/java/xiaozhi/modules/appv2/service/AppV2ServiceImplTest.java`
- 输入：
  - prompt / image / svg
- 输出：
  - single-line svg / `E_INVALID_DRAWING`
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=SingleLineSvgValidatorTest,AppV2ServiceImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

### M3.9 `draw_generated` 端到端

- 状态：设计期最小 `svg_text -> run_path`、`prompt -> local fake provider -> deterministic single-line SVG -> run_path`、`bitmap -> vectorize -> single-line SVG -> run_path` 与显式 `starter_* -> single-line SVG -> run_path` 链路已落地（2026-05-15）。`DrawGeneratedProjectionService` 支持通过 M3.8 校验的简单单线 SVG path（`M/L/Z`）投影为 `run_path params.feed/path`，并计算 `total_length_mm / estimated_seconds`；请求仅带 `prompt` 时会通过 `DrawGeneratedSvgProvider` 调用 `LocalFakeDrawGeneratedSvgProvider` 生成确定性单线、无填充、黑色 stroke 的占位 SVG，再复用同一校验/投影路径，metadata 标记 `draw_generated_prompt_placeholder_v1` 并记录 `provider=local_fake_ai`，提交前会校验默认 `ai_plan:local_fake_ai_plan_v1` entitlement，为后续替换真实 AI provider 留出可测边界；请求带 `bitmap_base64 / bitmap_data_uri / image_base64` 时会解码位图、二值化暗像素并扫描线生成单条 path，metadata 标记 `draw_generated_bitmap_vectorize_minimal_v1`。`StarterAssetCatalog` 目前内置 5 件 `starter_star/starter_house/starter_tree/starter_fish/starter_flower`，仅在请求显式携带 `use_starter_asset=true` 且指定 `starter_id/starter_asset_id/preset_id` 时进入兼容链路，metadata 标记 `draw_generated_starter_asset_v1` 与 `source=starter_asset`；未显式授权使用 starter 的请求仍直接返回 `E_INVALID_DRAWING:starter_asset_not_explicit`，未知 starter 返回 `starter_asset_not_found`，避免生成式主流程静默回退。`AppV2ServiceImpl.submitTask` 任务落库保留 `draw_generated`，DeviceServer 下发前会按设备 `workspace_mm` 和默认 safe margin 计算可写区，自动注入或裁剪 `canvas_width_mm / canvas_height_mm / origin_x_mm / origin_y_mm`，再使用 `capability=run_path`；显式 canvas 仍按 contain 策略缩放并居中。小程序设备详情页已提供最小“画图 Demo”，支持 prompt 提交和 5 件显式 starter 按钮，starter 请求会携带 `use_starter_asset=true`。结构化布局参数已支持：`size_hint=larger/smaller/more_margin` 调整默认 canvas 比例，`align/layout_hint=left/right/top/bottom/top_left/...` 控制落点。`prompt/svg_text/transcript` 入站内容审核已接入，显式 starter asset entitlement 已在提交前校验，小程序已映射 `E_NOT_ENTITLED` 友好提示；真实 AI 重生成、真实 AI plan 授权、完整自然语言解析和实机端到端仍未完成。
- 目录：
  - `manager-api/`
  - `manager-mobile/`
  - firmware/server chain
- 输入：
  - simple prompt / simple bitmap / simple svg
- 输出：
  - run_path task
- 验收命令：
  - `rtk powershell.exe -Command "mvn '-Dtest=DrawGeneratedProjectionServiceTest,SingleLineSvgValidatorTest,AppV2ServiceImplTest,DeviceServerMotionGatewayImplTest' test"`（工作目录：`server/xiaozhi-esp32-server/main/manager-api`）

---

## M4 语音触发 + 声纹

### M4.1 VAD + 唤醒词最小接入

- 目录：`firmware/u8-xiaozhi/`
- 主要文件：
  - board integration
  - ESP-SR init
- 输入：
  - mic stream
- 输出：
  - wake session start
- 验收命令：
  - `rtk rg "ESP-SR|VAD|wake" firmware/u8-xiaozhi`

### M4.2 fake ASR / fake intent

- 状态：已完成最小 fake provider（2026-05-15）
- 目录：`tools/fake_ai/`
- 主要文件：
  - `app.py`
- 输入：
  - audio/text mock
- 输出：
  - fixed transcript / Edge-B shaped intent / silent WAV TTS
- 验收命令：
  - `rtk python tools/fake_ai/app.py --help`
  - `rtk python -m unittest tools.fake_ai.tests.test_app -v`

### M4.3 DeviceServer intent 分流

- 目录：`xiaozhi-server/`
- 主要文件：
  - `core/`
  - provider adapter
- 输入：
  - ASR / LLM result
- 输出：
  - `chat_intent` / `device_intent`
- 验收命令：
  - `rtk rg "intent_submit|chat_intent|device_intent" server/xiaozhi-esp32-server/main/xiaozhi-server`

### M4.4 无声纹语音任务 demo

- 状态：设计期最小链路已接（2026-05-15）。`xiaozhi-server` deterministic 解析 `"小智，写你好"` / `"画一个星星"`，`manager-api` 新增 internal `/internal/v1/voice_task` 入口创建 `source=voice` task；实机完整执行仍未完成。
- 目录：
  - `xiaozhi-server/`
  - `manager-api/`
  - firmware chain
- 输入：
  - `"小智，写你好"`
- 输出：
  - `source=voice` task
- 验收命令：
  - `rtk rg "source=voice|intent_submit" server/xiaozhi-esp32-server/main`

### M4.5 声纹注册

- 状态：设计期最小资源域与注册入口已接（2026-05-15）。新增 `members` / `voiceprints` 表和 `POST /api/v1/voiceprints/enroll`，当前只用本地 fake 摘要落 `embedding_hash` / `speaker_ref`，不保存原始音频；真实声纹 API 与 DeviceServer 缓存推送未完成。
- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 主要文件：
  - voiceprint controller/service
  - member UI
- 输入：
  - 5~8s enrollment audio
- 输出：
  - `voiceprints` record
- 验收命令：
  - `./mvnw test`

### M4.6 实时比对最小闭环

- 状态：设计期缓存与入口策略门禁已接（2026-05-15）。`manager-api` 提供 internal `/internal/v1/voiceprints/cache`，`xiaozhi-server` 新增 `ActiveVoiceprintCache`、BusinessServer 缓存拉取和 ASR JSON `speaker_ref` 门禁；真实音频比对和真实 `speaker_ref` 产出未完成。
- 目录：`xiaozhi-server/`
- 主要文件：
  - active voiceprint cache
  - verification adapter
- 输入：
  - wake segment audio
- 输出：
  - pass / reject
- 验收命令：
  - `rtk rg "voiceprint" server/xiaozhi-esp32-server/main/xiaozhi-server`

### M4.7 三档模式

- 状态：设计期三档策略已接入语音入口（2026-05-15）。支持 `voiceprint_off / loose / strict` 的基础 allow/reject 判定，并可从 `voiceprint.mode` 或 `server.voiceprint_mode` 读取；儿童重录提示、真实音频比对接入未完成。
- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
- 输入：
  - `voiceprint_off / loose / strict`
- 输出：
  - mode-specific policy
- 验收命令：
  - `rtk rg "voiceprint_off|loose|strict" server/xiaozhi-esp32-server/main`

### M4.8 家庭成员最小管理

- 状态：设计期最小 BusinessServer API 已接（2026-05-15）。新增 `MemberService`、`POST /api/v1/members` 和 `POST /api/v1/devices/{deviceId}/members/list`，可在绑定设备下维护 active 的 `owner/member/child` 记录；前端完整入口、删除/禁用和儿童重录提醒未完成。
- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 输入：
  - owner/member/child
- 输出：
  - member records
- 验收命令：
  - `./mvnw test`

### M4.9 儿童兜底策略

- 状态：设计期最小策略已接（2026-05-15）。`V2VoiceprintCacheEntry` 携带 `expiresAt`，`xiaozhi-server` 支持 `child` 模式、`child_unknown_allowed` 兜底和 `child_reenroll_required` 提示标记；真实重录 UI/语音播报、家长二次确认和真实音频比对未完成。
- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - `manager-mobile/`
- 输出：
  - child mode fallback
- 验收命令：
  - `rtk rg "child" server/xiaozhi-esp32-server/main`

### M4.10 带声纹语音 demo

- 目录：全链路
- 输入：
  - registered parent voice
- 输出：
  - verified voice task
- 验收命令：
  - `rtk rg "voiceprint|source=voice" server/xiaozhi-esp32-server/main firmware`

---

## M5 OTA / 配网 / 自检

### M5.1 BLE 配网通道

- 目录：`firmware/u8-xiaozhi/`
- 主要文件：
  - provisioning integration
- 输入：
  - BLE credentials
- 输出：
  - Wi-Fi connected state
- 验收命令：
  - `rtk rg "provision|BLE|wifi" firmware/u8-xiaozhi`

### M5.2 AP 回退配网

- 目录：`firmware/u8-xiaozhi/`
- 输出：
  - fallback AP flow
- 验收命令：
  - `rtk rg "AP|provision" firmware/u8-xiaozhi`

### M5.3 NVS 加密存储

- 目录：`firmware/u8-xiaozhi/`
- 输出：
  - encrypted Wi-Fi / secret persistence
- 验收命令：
  - `rtk rg "nvs|encryption|device_secret" firmware/u8-xiaozhi`

### M5.4 OTA 客户端最小接入

- 目录：`firmware/u8-xiaozhi/`
- 输入：
  - release metadata
- 输出：
  - A/B upgrade flow
- 验收命令：
  - `rtk rg "ota|sha256|rollback" firmware/u8-xiaozhi`

### M5.5 OTA 后台最小接入

- 目录：`manager-api/`
- 主要文件：
  - firmware release service
- 输出：
  - release plan
- 验收命令：
  - `./mvnw test`

### M5.6 升级期任务门禁

- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - `u8-xiaozhi/`
- 输出：
  - `E_DEVICE_UPDATING`
- 验收命令：
  - `rtk rg "E_DEVICE_UPDATING|updating" server/xiaozhi-esp32-server/main firmware`

### M5.7 开机自检最小闭环

- 目录：
  - `firmware/u8-xiaozhi/`
  - `manager-api/`
- 输出：
  - self-check event
- 验收命令：
  - `rtk rg "self_check|diagnostic|uart" firmware/u8-xiaozhi server/xiaozhi-esp32-server/main`

### M5.8 小程序健康检查

- 目录：`manager-mobile/`
- 输出：
  - health check UI
- 验收命令：
  - `rtk corepack pnpm -C server/xiaozhi-esp32-server/main/manager-mobile build`

---

## M6 上线合规与运营级

### M6.1 隐私政策与权限授权

- 目录：`manager-mobile/src/`
- 主要文件：
  - auth/privacy pages
  - permission hooks
- 输出：
  - visible consent flow
- 验收命令：
  - `rtk corepack pnpm -C server/xiaozhi-esp32-server/main/manager-mobile build`

### M6.2 数据删除与敏感信息最小合规

- 目录：`manager-api/`
- 输出：
  - account delete
  - voiceprint delete
- 验收命令：
  - `./mvnw test`

### M6.3 备案与等保准备

- 目录：`docs/`
- 主要文件：
  - checklist docs
- 输出：
  - release checklist
- 验收命令：
  - `rtk rg "ICP|等保|PIPL|隐私" docs`

### M6.4 主控仲裁

- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 输出：
  - single writer policy
- 验收命令：
  - `./mvnw test`

### M6.5 设备转赠

- 目录：`manager-api/`
- 输出：
  - transfer flow
- 验收命令：
  - `./mvnw test`

### M6.6 耗材标记

- 目录：
  - `manager-api/`
  - `manager-mobile/`
- 输出：
  - supplies state UI + API
- 验收命令：
  - `rtk rg "paper_slot_state|pen_installed_at|pen_ink_percent_est" server/xiaozhi-esp32-server/main`

### M6.7 售后 RMA

- 目录：`manager-api/`
- 输出：
  - RMA state transitions
- 验收命令：
  - `./mvnw test`

### M6.8 监控告警

- 目录：
  - `manager-api/`
  - `xiaozhi-server/`
  - deployment/monitoring assets（待建）
- 输出：
  - metrics + alerts
- 验收命令：
  - `rtk rg "Prometheus|Grafana|alert" server/xiaozhi-esp32-server/main docs`

---

## 任务派发建议

适合直接派发给编码 agent 的最小单位：

1. 单文件或单模块变更
2. 单一输入输出契约
3. 单一验收命令
4. 不跨两个以上角色目录

建议优先派发顺序：

1. `M1.2 ~ M1.8`
2. `M2.1 ~ M2.8`
3. `M2.9 ~ M2.12`
4. `M3.*`
5. `M4.*`
6. `M5.*`
7. `M6.*`

不建议一次性派发的打包任务：

- `M2` 全部一起做
- `M3 + M4` 一起做
- `OTA + 配网 + 自检` 一起做
- `合规 + 运营 + 售后` 一起做
