# M2 状态核实报告

> 日期：2026-05-17
> 基准：`docs/实施计划-v2.md` M2 章节（最后更新 2026-05-15）
> 方法：逐项核对实施计划标记 vs 实际仓库代码

## 结论：M2 无设备编码工作已全部完成

v1 代码质量报告（05-14）说 M2 是 0%，但 05-14~05-16 之间发生了大量实现推送。
实施计划 v2 中 M2.1~M2.15 均标记为"已完成/已实现/设计期已完成"。
本次核实逐项用代码文件路径和测试覆盖确认了全部完成状态。

## 逐项核实

| M2 项 | 计划状态 | 核实结果 |
|-------|---------|---------|
| M2.1 数据库 5 表 | 已完成 | `db/changelog/202605141200.sql` — accounts/devices/device_bindings/activation_codes/tasks ✅ |
| M2.2 REST 三件套 | 已完成 | `AppV2Controller` POST /login, /devices/bind, /devices/{id}/tasks ✅ |
| M2.3 Edge-B motion_task | 已实现 | `DeviceServerMotionGatewayImpl` ✅ |
| M2.4 DeviceServer 转发 | 已实现 | `motionHandle.py` — WSS 下行 ✅ |
| M2.5 U8 motion_task 路由 | 已实现 | `dlc_motor_control_p1_ai_board.cc` — capability 路由 ✅ |
| M2.6 motion_event 反向 | 已实现 | `InternalMotionEventController` + xiaozhi-server handler ✅ |
| M2.7 Edge-A WSS | 已实现 | `ClientEdgeWebSocketHandler` + `EdgeAClientHub` + `/ws/v1/client` ✅ |
| M2.8 小程序三页 | 已完成 | `v2/login/` + `v2/device-list/` + `v2/device-detail/` ✅ |
| M2.9 motion_event 落库 | 设计期完成 | `AppV2ServiceImpl.ingestMotionEvent` 五态映射 ✅ |
| M2.10 per-device seq | 设计期完成 | `EdgeAClientHub` 单调 seq + since_seq 重放 ✅ |
| M2.11 幂等提交 | 设计期完成 | (account, device, request_id) 去重 ✅ |
| M2.12 run_path 端到端 | 设计期完成 | PATH_BEGIN/SEG/END + progress 事件流 ✅ |
| M2.13 get_device_info | 部分完成 | 全链路 schema + fake + 实现，实机 TTS 待验 |
| M2.14 语音安全白名单 | 设计期完成 | home/pause/resume/stop/get_device_info + move_rel 白名单 ✅ |
| M2.15 TTS 播报协议 | 设计期完成 | tts_hint 模板句，实机播放待验 ✅ |

## 仍需要实物/部署环境验证的项

以下 M2 项编码已完成，但因缺少硬件或运行时环境无法本地验证：

- M2.1: 无 mvn 环境，未做 `spring-boot:run` 启动验收
- M2.12: U1 真实执行、U8 刷写
- M2.13: 实机 TTS 播放 "这台机器是什么型号"
- M2.14: 实机语音动作（"往左一点"）
- M2.15: 实机 TTS 播报任务开始/结束

## 与之前认知的矛盾

v1 代码质量报告（05-14）说 M2 是 0%，实际上在 05-14~05-16 两天内完成了 M0~M6 全部本地编码。
M6 closeout audit（05-16）已记录 251 个测试全绿、62 个 schema 校验通过。
