# 接续指令

> 本文件用于跨会话接续。当前会话发生上下文较深的工作后按 Superpowers §3bis "宁早切窗口不带病工作" 纪律主动切窗。新窗口读完此文件即可接续。

## 全局规则已更新（新窗会自动加载）

`C:\Users\zhugu\.claude\CLAUDE.md` 已写入以下强制规则，新窗口会自动加载执行：

- §1 严苛遵循 Superpowers 原则（五步顺序 + 六条纪律）
- §2 运行环境：Windows 11 + Git Bash（路径/命令约束 7 条）
- §3 上下文超 20% 强制压缩
- §3bis 记忆混乱自检与新开窗口提示（6 条征兆 + 三步接续）
- §4 跨项目通用约束（不推密钥 / 库升级独立 commit）
- §5 编码阶段性验证后必须及时更新文档（5 种节点 + 6 类文档 + 4 步顺序）

以上条款都是"强制执行"，不是建议。

## 当前里程碑位置

参考 `docs/实施计划-v2.md`，当前位于：

- **M0e 基准修复**：已完成（UART TX/RX 修复、硬件文档证据强度标注、v1 失效声明）
- **M0a 契约固化（JSON Schema）**：未开始，task #57 pending
- **M0b 静态检查（GPIO 检查器）**：未开始，task #58 pending
- **M0c 仿真器（fake U1 等）**：未开始，task #61 pending
- **M0d CI 骨架**：未开始，task #60 pending
- **M0f 实物上电抽测**：等实物
- **M0g 硬件核对报告**：进行中，见下节

## 硬件核对报告进度（轮 1 已合入主分支）

目标：`docs/硬件核对报告.md`，按 L1 引脚 / L2 配置 / L3 应力三级核对 13 类芯片。

### 已完成（轮 1，2026-05-14）

- §0 方法论：三级核对制 + 证据强度 D1~DX + 风险级别 L1/L2/L3
- §1.1 ESP32-S3-WROOM-1-N16R8 strapping pin 全量核对：IO0/IO3/IO45/IO46
- §A 风险登记表初版：R-001 ~ R-006
- §A.2 待覆盖的器件清单（按轮次分配）

### 待完成（按计划顺序）

- 轮 2：§1.2 HR4988E（DIR/STEP 时序、EN 极性、微步配置）+ R-006 PCB 走线核实 + R-001 PCB 下拉核实；旧 `TMC2208-*` 仅为网络名/元件属性遗留
- 轮 3：§1.3 ES8311 / §1.4 ES7210（从 ESP-IDF 组件源码反查）
- 轮 4：§1.5 摄像头 + 2V8 LDO（先识别型号）
- 轮 5：§1.6~§1.8 HX711 / CH340 / PC357N3
- 轮 6：§1.9~§1.13 电源系列 + IRFZ44N / SS36 / FNR4030


## 新窗口接续指令（可直接复制）

```
接续 esp32S_XYZ 项目的硬件核对报告 M0g 工作（轮 2）。

先读：
1. C:\Users\zhugu\.claude\CLAUDE.md（全局强制规则）
2. docs/接续指令.md（本文件）
3. docs/架构定稿-v2.md（基准架构）
4. docs/实施计划-v2.md（里程碑位置）
5. docs/硬件连接与GPIO分配说明.md（硬件基准）
6. docs/硬件核对报告.md（轮 1 已落 §0 + §1.1 + §A）

按 Superpowers §1 "前置条件先验证"，轮 2 任务：
- §1.2 HR4988E datasheet 核对；SCH PDF 第 4 页与 BOM 已确认 U2/U3/U4/U52 实际型号为 HR4988E，旧 TMC2208-LA-T 仅保留在 PADS `.txt` 元件属性/网络名中
- 核 R-006：解析 ESP32-S3 datasheet PDF §3.5 表 3-3 / 模组 pin list，回答 N16R8 模组对外是否引出 IO38/39/40，并据此回填 §1.1.6
- 核 R-001 PCB：解析 PCB PDF 看 IO3 附近是否已有外部下拉电阻
- 写完 §1.2 + §1.1.6 修订 + R-006 / R-001 状态更新后立即 commit + push

仍按全局 §5 "阶段性验证后即更新文档"，文档与代码同 commit。
如遇 datasheet 拿不到，诚实标注 [DX] 数据源不可得，不编造参数。
```


## 本会话工作成果（已在 GitHub main）

commit 链：

```
7c40e0d  Split M0 into design-time verification phases
cc1fc1f  Upgrade arduinoWebSockets from 2.1.2 to 2.7.1
f54a4b7  Fix U8 UART TX/RX swap and align GPIO doc with PDF evidence
af82d65  Define v2 architecture with client/business/device split
```

本会话还有未提交的工作（本文件本身）在下次 commit 一并推送。

## 待办总表

按优先级：

1. **M0g 硬件核对报告**（本轮切窗原因）—— 新窗继续
2. **M0a JSON Schema** —— 需 M0g 完成或至少 §1 章节完成后启动，因为 schema 里需要引用硬件真相
3. **M0b GPIO 检查器** —— 并行可做，不依赖硬件核对
4. **M0c Fake U1** —— 依赖 M0a schema
5. **M0d CI 骨架** —— 依赖前三项有内容可跑

Superpowers 顺序：**M0g → M0a → M0b → M0c → M0d → M1**

## 当前状态自评

- 记忆未出现全局 §3bis 所述的 6 条混乱征兆
- 但本轮已处理多方向（v2 多次扩充、库升级、硬件核对部分、全局规则三次更新），**主动切窗是纪律动作**
- 新窗从本文件接续，不需要读完完整会话历史
