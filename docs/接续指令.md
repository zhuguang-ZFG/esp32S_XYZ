# 接续指令

> 本文件用于跨会话接续。当前会话发生上下文较深的工作后按 Superpowers §3bis "宁早切窗口不带病工作" 纪律主动切窗。新窗口读完此文件即可接续。

## 全局规则已更新（新窗会自动加载）

`C:\Users\zhugu\.claude\CLAUDE.md` 已写入以下强制规则，新窗口会自动加载执行：

- §1 严苛遵循 Superpowers 原则（五步顺序 + 六条纪律）
- §2 运行环境：Windows 11 + Git Bash（路径/命令约束 7 条）
- §3 上下文超 20% 强制压缩
- §3bis 记忆混乱自检与新开窗口提示（6 条征兆 + 三步接续）
- §4 跨项目通用约束（不推密钥 / 库升级独立 commit）
- §5 编码阶段性验证后必须及时更新文档（5 种节点 + 6 类文档 + 4 步顺序）

以上条款都是"强制执行"，不是建议。

## 当前里程碑位置

参考 `docs/实施计划-v2.md`，当前位于：

- **M0e 基准修复**：已完成（UART TX/RX 修复、硬件文档证据强度标注、v1 失效声明）
- **M0a 契约固化（JSON Schema）**：已完成（2026-05-15）
- **M0b 静态检查（GPIO 检查器）**：已完成（2026-05-15）
- **M0c 仿真器（fake U1 等）**：已完成（2026-05-16 扩展 M5/M6 路由）
- **M0d CI 骨架**：已完成本轮 workflow 更新（2026-05-16 补入 AESUtilsTest）；待远端跑绿
- **M0f 实物上电抽测**：等实物
- **M0g 硬件核对报告**：§1 设计期核对已覆盖 13 类器件，风险项待 M0f/后续闭环
- **M1.0 Edge-D 固件静态契约收口**：已完成
- **M1 设备协议端到端**：已完成（代码、测试、文档）
- **M2 小程序订阅链路**：已完成（代码、测试、文档），详情见 `docs/M2-状态核实-2026-05-17.md`
- **M3 投影管线**：已完成（设计期最小链路，含 write_text/draw_generated），详情见实施计划 v2
- **M4 语音触发+声纹**：已完成（设计期最小链路，含 deviceIntentMap），详情见实施计划 v2
- **M5 OTA/配网/自检**：设计期证据契约已落地（2026-05-16），Open Gate 等实物
- **M6 上线合规与运营级**：设计期证据契约已落地（2026-05-16），Open Gate 等生产环境

## 硬件核对报告进度（轮 6 已完成）

目标：`docs/硬件核对报告.md`，按 L1 引脚 / L2 配置 / L3 应力三级核对 13 类芯片。

### 已完成（轮 1，2026-05-14）

- §0 方法论：三级核对制 + 证据强度 D1~DX + 风险级别 L1/L2/L3
- §1.1 ESP32-S3-WROOM-1-N16R8 strapping pin 全量核对：IO0/IO3/IO45/IO46
- §A 风险登记表初版：R-001 ~ R-006
- §A.2 待覆盖的器件清单（按轮次分配）

### 已完成（轮 2，2026-05-14）

- §1.2 HR4988E：完成型号、DIR/STEP 时序、EN 极性、MS1/MS2/MS3 微步、限流估算、ROSC/CP 电容核对
- 固件 `dlc_motor_control_p1.h` 新增 `STEP_PULSE_DELAY=1 us`，给 HR4988E DIR setup 留明确余量
- R-001：PADS `.txt` 证实 `TMC2208-1_DIR` / U1.IO3 无外部下拉，风险保持 open
- R-006：PADS `.txt` + ESP32-S3-WROOM-1 datasheet 双证据确认 U8.IO38/39/40 可用，保持 closed
- 新增 R-007/R-008：HR4988E `ROSC=1K` 与 CP1/CP2 `22nF` 偏离 datasheet 推荐，需硬件确认或改版

### 已完成（轮 3，2026-05-15）

- §1.3 ES8311：完成 PADS 元件/网络与 U8 固件配置核对，纠正 `U12=ES8311`，确认 I2C/I2S/电源网络与固件 pin 定义一致
- §1.4 ES7210：完成 PADS 元件/网络与 U8 固件 TDM 配置核对，纠正 `U11=ES7210`，确认 `AD0=1, AD1=0` 与固件 `0x82` 地址写法一致
- 新增 R-009/R-010：ES8311 PA 爆音时序、去耦/时钟外围需 datasheet 或实机验证
- 新增 R-011/R-012/R-013：ES7210 MIC4 外部入口、MICBIAS/输入外围、电源树上电顺序需后续闭环
- 本轮未 commit/push，等待用户明确允许

### 已完成（轮 4，2026-05-15）

- §1.5 摄像头 + 2V8 LDO：确认 `J11=AXK724147G` 是 24 pin 连接器，不是 sensor 本体；真实摄像头 sensor/module 型号登记 `[DX]`
- 确认 `U56=ME6211C28M5G`，`+3V3 -> U56 -> 2V8 -> J11.11`，输入/输出去耦与 `R119=10K` PWDN 上拉已记录
- U8 固件 DVP pin map 与 PADS `J11` 网络一致：`D0~D7`、`VSYNC/HREF/PCLK/XCLK`、`PWDN`、`SDA/SCL` 均可逐项对上
- `R-002/R-005` 更新为 `open-with-mitigation`：`DVP_PWDN` 上拉提供缓解，但仍缺真实 sensor 在 `PWDN=1` 时 VSYNC/HREF 输出态和上电波形
- 新增 R-014/R-015/R-016：摄像头 sensor 型号未知、`AI_RESET` 未由 esp-camera 主动控制、2V8 负载余量/启动浪涌未知
- 本轮未 commit/push，等待用户明确允许

### 已完成（轮 5，2026-05-15）

- §1.6 HX711：确认 `U15=HX711`，`PD_SCK -> HX711_SCLK -> U1.IO7`，`DOUT -> HX711_MISO -> U1.IO15`；`J10` 为四线桥式压力/称重传感器接口
- §1.7 CH340C：确认 `U9/U10=CH340C`，分别对应 U1/U8 本机 Type-C USB-UART；D+/D-、TX/RX、3.3V 供电和 DTR#/RTS# 自动下载拓扑均有 PADS 证据
- §1.8 PC357N3：确认 `U171/U5/U6/U7=PC357N3`，四路 `UUT_SENSOR_1~4` 分别接 U1 `GPIO9/12/13/14`，对应 X/Y/Y2/Z limit
- 新增 R-017/R-018/R-019：HX711 传感器/标定/噪声未闭环、CH340C 双路自动下载时序未实测、PC357N3 传感器极性/CTR/装配选项需实物闭环
- 本轮未 commit/push，等待用户明确允许

### 已完成（轮 6，2026-05-15）

- §1.9 ME3116：确认 `U22=ME3116` 生成 `+3V3`、`U14=ME3116` 生成 `AU_3V3`，两路 buck 的 PADS 拓扑、反馈电阻、输入/输出路径已记录
- §1.10 LM1117 / LM317：确认 `U13=LM1117` 从 `USB5V_VCC` 辅助生成 `+3V3`，`U32=LM317_ADJ` 从 `12V` 生成 `+5V_A`
- §1.11 IRFZ44N：确认 `Q5=IRFZ44NPBF` 为 J12 激光/12V 负载低边开关，`LASER_CONTROL -> R121=47 -> Q5.G`，`R120=100K` 下拉
- §1.12 SS36：确认 `S1/S2/S7/S8/S9=SS36`，分别用于 buck SW/输入隔离与 USB5V 到 U13 输入路径
- §1.13 FNR4030：确认 `L1/L2=FNR4030S220MT`，分别属于 `AU_3V3` 与 `+3V3` buck 输出路径
- 新增 R-020~R-024：`+3V3` 双来源反灌、`+5V_A` LM317 热耗散、buck 电感/肖特基/ME3116 余量、IRFZ44N 3.3V 栅压导通、J12 引脚编号差异
- 本轮未 commit/push，等待用户明确允许

### 待完成（按计划顺序）

- M0c：fake U1 / fake DeviceServer / fake AI 仿真器（task #61）本轮已完成最小闭环
- M0d：CI 骨架（task #60）本轮已完成本地等价验证，待远端 Actions 跑绿确认
- M1.0：Edge-D 固件静态契约收口已完成本地验证，待后续远端 Actions 与实物链路确认

## M0a JSON Schema 进度（已完成，2026-05-15）

- `docs/schemas/` 当前包含 26 个 JSON Schema 与 36 个 example，覆盖 Edge-A/B/C/D。
- 补齐 Edge-A 服务端实际响应：`authed`、`acked`、`unsubscribed` 的 schema 与 example。
- 补齐 Edge-D `README.md`；`cmd.schema.json` 已覆盖 v2 §5.4 命令集里的 `GET_DEVICE_INFO`。
- M2.13 `get_device_info` 已推进到小程序设备详情页：可提交 `get_device_info`，并在 Edge-A `device_info_reply` 到达后展示 `model / hw_rev / fw_rev / workspace_mm`；新增 `tests/ci/test_manager_mobile_device_info.py` 静态守护。真实 DeviceServer 收到 `device_info` 后已把型号/版本/工作空间摘要入队 TTS；自然语言意图映射和实机播放仍待 M2.14/M2.15 验证。
- M2.14 已新增 U8 `self.motor.pause/resume/stop` 设备 MCP 工具和 motion_task 分支；真实 xiaozhi-server 的确定性语音映射覆盖 `型号/设备信息 -> self.motor.get_device_info`、`归零/回原点 -> self.motor.home`、`暂停 -> self.motor.pause`、`继续/恢复 -> self.motor.resume`、`停止/停下 -> self.motor.stop`，且只在对应设备 MCP 工具已注册时生效；新增 `tests/ci/test_xiaozhi_server_device_intent_map.py`。U8 已新增 `self.motor.move_rel` 小步白名单，语音只映射“往左/右/前/后/上/下/抬高/降低一点”等明确相对移动短语，每轴限制 `-1/0/1mm`，执行前读取 `GET_STATUS` 当前位置和 `GET_DEVICE_INFO` 工作空间，超步长、零步长、越界或无法验证工作空间时拒绝；U8 构建和实机动作仍待 ESP-IDF / 硬件环境补验。
- M2.15 设计期最小链路已补齐：U8 `motion_event` 透传 `source=voice|client`；xiaozhi-server 按 `accepted/done/failed/rejected` 生成并入队 `tts_hint.tts_event=task_started|task_done|task_failed|task_rejected`，`running/progress` 不播报；`device_info` 生成 `device_info_reply` TTS hint；BusinessServer Edge-A 广播保留 `job_status.payload.tts_hint` 和 `device_info_reply.payload.tts_hint`；实机 TTS 播放待硬件环境补验。
- M2.9~M2.11 设计期已完成：M2.9 `ingestMotionEvent` 会落 `status/started_at/finished_at/error/result_json`，`rejected` 归入失败终态；M2.10 `EdgeAClientHub` 已有 per-device 内存 `seq`、topic buffer、`since_seq` replay 和 `ack` 单调确认；M2.11 `submitTask` 已按 `(account_id, device_id, request_id)` 幂等返回既有 `task_id/status`，不重复转发 DeviceServer。持久化 seq 与前端最近任务展示仍待后续生产化/页面闭环。
- 已用 `jsonschema` 校验全部 schema 与样例；验证命令输出 `validated=62 passed=62 failed=0`。
- 本轮未 commit/push，等待用户明确允许。

## M0b GPIO 静态检查器进度（已完成，2026-05-15）

- `tools/check_gpio.py` 当前仓库输出 `OK: GPIO check passed; no issues found`。
- 修复 `LIMIT` 输入识别、I2C `SDA/SCL` 误判、U8 UART TX/RX 独立守卫。
- 更新 `tools/tests/test_check_gpio.py` 到当前 `GPIOChecker` API，补充 OUTPUT 冲突行号、UART swap、未引出 GPIO 等断言。
- 验证通过：`rtk python tools/test_check_gpio.py -v`（8 tests OK）与 `rtk python -m unittest tools.tests.test_check_gpio -v`（7 tests OK）。
- 本轮未 commit/push，等待用户明确允许。


## 新窗口接续指令（可直接复制）

```
接续 esp32S_XYZ 项目。

当前状态：M0~M6 无设备编码已全部完成（251 tests + 62 schema + 188 Java tests），等待 commit/push 触发远端 CI。

下一步选择：
  A) commit/push 触发远端 GitHub Actions → 确认 CI 全绿
  B) 树莓派/服务器部署 → 打通 manager-api + MySQL + Redis 后端链路
  C) 等实物 → M0f 上电抽测

先读：
1. C:\Users\zhugu\.claude\CLAUDE.md（全局强制规则）
2. docs/接续指令.md（本文件）
3. docs/架构定稿-v2.md（基准架构）
4. docs/实施计划-v2.md（里程碑位置，M2~M6 均已标注完成状态）
5. docs/schemas/README.md（62 项全部通过）
6. docs/M2-状态核实-2026-05-17.md（M2 逐项核实）
7. docs/code-quality-audit-2026-05-17.md（最新质量审计）
8. docs/硬件连接与GPIO分配说明.md + docs/硬件核对报告.md

按 Superpowers §1 "前置条件先验证"，做任何事之前先确认对应测试通过（python -m unittest discover -s tests/ci）。
```


## 本会话工作成果（已在 GitHub main）

commit 链：

```
7c40e0d  Split M0 into design-time verification phases
cc1fc1f  Upgrade arduinoWebSockets from 2.1.2 to 2.7.1
f54a4b7  Fix U8 UART TX/RX swap and align GPIO doc with PDF evidence
af82d65  Define v2 architecture with client/business/device split
```

本会话还有未提交的工作（硬件核对报告、轮 3/4/5/6 计划、M0a/M0b/M0c/M0d/M1.0 计划、schema 文件、GPIO 检查器/测试、fake 工具、CI、固件静态契约修正、本文件）在下次 commit 一并推送。

## 待办总表

按优先级：

1. **commit/push 触发远端 CI** —— 当前所有本地验证通过（251 tests + 62 schema + 188 Java tests）
2. **树莓派/服务器部署** —— manager-api + MySQL + Redis，打通登录/绑定/归零链路
3. **M0f 实物上电抽测** —— 等实物；需要闭环 `R-001~R-024`
4. **M1~M6 实机联调** —— 所有 "Open Gate" 都需要实物硬件或生产环境

Superpowers 顺序：**commit/push 触发远端 CI → 部署打通后端链路 → M0f 实物轨 / M1~M6 实机联调**

## 当前状态自评

- 记忆未出现全局 §3bis 所述的 6 条混乱征兆
- 本轮工作量适中（CI 补齐 + fake DeviceServer 扩展 + 全量验证）
- 所有本地验证通过：246 CI tests + 31 fake tool tests + 76 Java tests + type-check + build
- 新窗从本文件接续，不需要读完完整会话历史
